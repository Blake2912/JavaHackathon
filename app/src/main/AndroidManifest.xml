<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.applicationtoconnectpeople">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApplicationToConnectPeople">
        <activity android:name=".HospitalBedBloodBankActivity"></activity>
        <activity android:name=".NGOActivity" />
        <activity android:name=".HospitalBedListsActivity" />
        <activity android:name=".OptionChooseActivity" />
        <activity android:name=".RegistrationPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>